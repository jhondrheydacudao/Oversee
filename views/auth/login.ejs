<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pterodactyl - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #0f131a;
      font-family: 'General Sans', sans-serif;
    }
    .glass-bg {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
    }
    .input-field {
      background-color: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    .input-field:focus {
      border-color: #8c5bff;
      outline: none;
      box-shadow: 0 0 5px #8c5bff;
    }
    .login-btn {
      background: linear-gradient(90deg, #8c5bff, #6a42cc);
    }
    .login-btn:hover {
      background: #6a42cc;
    }
    .footer-text {
      font-size: 0.85rem;
      color: rgba(255, 255, 255, 0.5);
    }
  </style>
</head>

<body class="min-h-screen flex flex-col items-center justify-center">
  <main class="max-w-md w-full p-8 rounded-lg glass-bg text-white">
    <h2 class="text-center text-2xl font-semibold">Sign in to Pterodactyl</h2>
    
    <!-- Login Form -->
    <form method="POST" action="/auth/login" class="mt-6 space-y-4">
      <input type="hidden" name="_token" value="{{ csrf_token() }}">
      
      <div>
        <input type="text" 
        id="username" 
        name="username" 
        class="input-field w-full p-3 rounded-md text-white placeholder-gray-400" 
        placeholder="Username or Email" 
        required>
      </div>

      <div>
        <input type="password" 
        id="password" 
        name="password" 
        class="input-field w-full p-3 rounded-md text-white placeholder-gray-400" 
        placeholder="Password" 
        required>
      </div>

      <button type="submit" class="login-btn w-full text-white font-semibold py-3 rounded-md transition duration-300">                     
        Sign in
      </button>
    </form>

    <!-- Footer -->
    <p class="text-center footer-text mt-6">
      Made with <a href="https://pterodactyl.io" target="_blank" class="text-indigo-400 hover:underline">Pterodactyl</a>
    </p>
  </main>
</body>
</html>
