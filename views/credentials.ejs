<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Credentials - <%= name %></title>

    <!-- Link to Tailwind CSS and custom styles -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/assets/styles.css"> <!-- Correct path to CSS file -->

    <!-- Add Favicon for Browser Tab -->
    <link rel="icon" href="https://yt3.googleusercontent.com/IVqzx0L3Ffpapttg-WK5yU9frk4AIVKixHnmmQjQ7RLrt4e66-7Ox6srKG_QVat5ceEq-98-s9s=s160-c-k-c0x00ffffff-no-rj" type="image/png">

    <!-- Add animation CSS -->
    <style>
        /* Animation for fading in */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Apply the animation to the body or container */
        body, .container {
            animation: fadeIn 1s ease-out;
        }
    </style>
</head>
<body class="bg-dark min-h-screen flex items-center justify-center py-10">
    <!-- Fixed Back to Dashboard Button -->
    <a href="/dashboard" class="fixed top-4 right-4">
        <button class="bg-gradient-to-r from-indigo-500 to-purple-600 text-white font-semibold py-3 px-6 rounded-md shadow-lg hover:bg-indigo-400 transition duration-300">
            ⬅ Back to Dashboard
        </button>
    </a>

    <div class="container bg-glass p-10 rounded-lg shadow-lg">
        <!-- Header -->
        <header class="text-center mb-6">
            <h1 class="text-3xl font-semibold text-white">🔐 Your Credentials</h1>
        </header>

        <!-- User Info -->
        <section class="mb-6">
            <p class="text-xl text-gray-300"><strong>Username:</strong> <%= user.username %></p>
            <p class="text-xl text-gray-300"><strong>Email:</strong> <%= user.email %></p>
        </section>

        <!-- Reset Password Form -->
        <section class="mb-6">
            <form action="/credentials/reset" method="POST">
                <button type="submit" class="w-full bg-gradient-to-r from-indigo-500 to-purple-600 text-white font-semibold py-3 rounded-md transition duration-300 hover:bg-indigo-400">Reset Password 🔄</button>
            </form>
        </section>

        <!-- Success/Error Message -->
        <section>
            <% if (req.query.msg === "PasswordReset") { %>
                <p class="text-green-500 text-lg font-semibold">✅ Your password has been reset. Check your email.</p>
            <% } else if (req.query.err) { %>
                <p class="text-red-500 text-lg font-semibold">⚠️ Error: <%= req.query.err %></p>
            <% } %>
        </section>
    </div>
</body>
</html>
