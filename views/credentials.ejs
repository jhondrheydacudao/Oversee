<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Credentials - <%= name %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>🔐 Your Credentials</h1>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Email:</strong> <%= user.email %></p>

        <form action="/credentials/reset" method="POST">
            <button type="submit">Reset Password 🔄</button>
        </form>

        <% if (req.query.msg === "PasswordReset") { %>
            <p style="color: green;">✅ Your password has been reset. Check your email.</p>
        <% } else if (req.query.err) { %>
            <p style="color: red;">⚠️ Error: <%= req.query.err %></p>
        <% } %>
        
        <a href="/dashboard">⬅ Back to Dashboard</a>
    </div>
</body>
</html>
